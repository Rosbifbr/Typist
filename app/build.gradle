apply plugin: 'com.android.application'

android {
    defaultConfig {
        applicationId "rosbifbr.typist.inputmethod"
        compileSdk 35
        minSdkVersion 19
        targetSdkVersion 35
        versionCode 108
        versionName "5.22"
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    namespace 'rosbifbr.typist.inputmethod'
}

dependencies {
}
